
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // During development, allow any authenticated user to perform actions.
    // WARNING: These rules are not secure and should be tightened before production.

    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
